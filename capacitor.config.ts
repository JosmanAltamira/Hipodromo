import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hipodromo.app',
  appName: 'Hipodromo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
